name: saudi_verify
description: A Dart package for validating Saudi Arabian identifiers — National ID, Iqama, mobile numbers, IBAN, and VAT numbers.
version: 0.1.1
repository: https://github.com/aliawami/saudi_verify

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
